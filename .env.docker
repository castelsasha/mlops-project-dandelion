########################################
# In-container paths (mounted volumes)
########################################
LOCAL_DATA_DIR=/data/raw            # Container path to raw data
PROCESSED_DATA_DIR=/data/processed  # Container path to processed data
LOCAL_MODEL_DIR=/models/            # Container path to local model cache

########################################
# MinIO from inside Docker network (use service name, not localhost)
########################################
S3_DATA_BUCKET=plant-images
S3_MODEL_BUCKET=plant-models
S3_ENDPOINT_URL=http://minio:9000   # 'minio' is the Docker service name
S3_ACCESS_KEY=minioadmin
S3_SECRET_KEY=minioadmin

########################################
# MLflow from inside Docker network
########################################
MLFLOW_TRACKING_URI=http://mlflow:5000
MLFLOW_EXPERIMENT_NAME=dandelion_vs_grass
MLFLOW_S3_ENDPOINT_URL=http://minio:9000
AWS_ACCESS_KEY_ID=minioadmin
AWS_SECRET_ACCESS_KEY=minioadmin
MLFLOW_ARTIFACT_ROOT=s3://plant-models/mlflow/

########################################
# API / Webapp (service-to-service URLs)
########################################
API_URL=http://api:8000/predict        # 'api' is the FastAPI service name
WEBAPP_API_URL=http://api:8000/predict # Streamlit calls the API via service name

########################################
# MySQL inside Docker network (optional SQL task)
########################################
MYSQL_HOST=mysql        # 'mysql' is the DB service name
MYSQL_PORT=3306
MYSQL_DB=mlops
MYSQL_USER=mlops
MYSQL_PASSWORD=mlops