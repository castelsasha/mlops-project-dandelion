apiVersion: v1
kind: Service
metadata:
  name: dandelion-api
  namespace: dandelion
spec:
  selector:
    app: dandelion-api
  type: LoadBalancer
  ports:
    - name: http
      protocol: TCP
      port: 8000
      targetPort: 8000
