# -----------------------------------------------------------------------------
# Ignore files that should never be committed to the repository.
# Avoid committing local data, credentials, and transient build outputs.
# -----------------------------------------------------------------------------

# Python caches
__pycache__/
*.pyc

# Local environment & secrets
.venv/
.env

# Local data & models (artifacts regenerated by pipelines)
data/
models/
mlruns/
mlflow.db
minio-data/
logs/
tmp/

# Streamlit local state
.streamlit/

# Docker overrides / env variants
*.env
docker-compose.override.yml

# Notebooks cache
.ipynb_checkpoints/

# Editor configs
.vscode/